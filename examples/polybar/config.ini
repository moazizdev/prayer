; Polybar module for Prayer Times
; Add this to your polybar config

[module/prayer]
type = custom/script
exec = prayer
interval = 60
label = 🕌 %output%
format-foreground = #a3be8c

; Alternative: Show only prayer name
[module/prayer-name]
type = custom/script
exec = prayer --name
interval = 60
label = 🕌 %output%

; Alternative: Show prayer with progress bar
[module/prayer-progress]
type = custom/script
exec = echo "$(prayer --name) $(prayer --progress)%"
interval = 60
label = 🕌 %output%
